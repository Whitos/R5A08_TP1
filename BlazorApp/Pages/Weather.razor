@page "/produits"
@inject IService<Produit> produitService
 
<PageTitle>Produits</PageTitle>

<h1>Liste des produits</h1>

@if (produits == null)
{
    <p><em>Chargement...</em></p>
}
else if (!produits.Any())
{
    <p><em>Aucun produit trouvé.</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Description</th>
                <th>Marque</th>
                <th>Type de Produit</th>
@*                 <th>Photo</th> *@
            </tr>
        </thead>
        <tbody>
            @foreach (var produit in produits)
            {
                <tr>
                    <td>@produit.NomProduit</td>
                    <td>@produit.Description</td>
                    <td>@produit.IdMarque</td>
                    <td>@produit.IdTypeProduit</td>
                   @*  <td>
                        <img src="@produit.UriPhoto" alt="@produit.NomProduit" width="100" />
                    </td> *@
                    <td>
                        <a class="btn btn-sm btn-warning" href="/edit-produit/@produit.IdProduit">Modifier</a>
                        @* <button class="btn btn-sm btn-danger" @onclick="() => DeleteProduit(produit.IdProduit)">Supprimer</button> *@
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private List<Produit>? produits;

    protected override async Task OnInitializedAsync()
    {
        produits = await produitService.GetAllAsync();
    }
}
